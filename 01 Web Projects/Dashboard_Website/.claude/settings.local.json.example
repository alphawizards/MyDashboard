{
  "_comment": "Copy this to settings.local.json (gitignored). Override shared settings for your local environment.",
  "permissions": {
    "allow": [
      "Bash(git push*)"
    ]
  },
  "env": {
    "RETIREAU_ALLOW_PII_READ": "0"
  }
}
